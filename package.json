{
  "name": "bittrex",
  "version": "0.0.0",
  "description": "An adapter to connect Via to the Bittrex exchange.",
  "main": "./lib/main",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "via",
    "trading",
    "bittrex"
  ],
  "exchange": "bittrex",
  "repository": "https://github.com/via-platform/bittrex",
  "author": "Dakota Gordon",
  "engines": {
    "via": "*"
  },
  "dependencies": {
    "axios": "^0.16.2",
    "underscore-plus": "^1.6.6",
    "uuid": "^3.1.0",
    "ws": "^3.3.3"
  },
  "configSchema": {},
  "accountConfigSchema": {
    "accountName": {
      "type": "string",
      "default": "Bittrex Trading Account",
      "description": "Give this account a short name for your own personal reference."
    },
    "apiKey": {
      "title": "API Key",
      "type": "string",
      "default": "",
      "description": "The API key generated by Bittrex."
    },
    "apiSecret": {
      "title": "Secret Key",
      "type": "string",
      "default": "",
      "description": "The API secret key generated by Bittrex."
    }
  }
}
